# Behavior

On startup, the title of the app should be "Sizzle".

As the user clicks on and activates different projects, the title of the app
should update to include the name of the currently focused project, e.g.
"Sizzle - MyProject"
